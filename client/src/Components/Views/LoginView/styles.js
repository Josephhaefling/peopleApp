import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    loginPage: {
        alignItems: 'center',
        display: 'flex',
        height: '85vh',
        justifyContent: 'center'
    },
}))