import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    eventContainer: {
        display: 'flex',
        flexDirection: 'column',
    }, 
    titleInput: {
    },
    descriptionInput: {
    },
    timeContainer: {
        display: 'flex'
    },
    selector: {
    }

}))