import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    eventView: {
        alignItems: 'center',
        display: 'flex',
        flexDirection: 'column',
        width: '100vw'
    }
}))